--- /home/subvadla/amd-master/auto-gpu-kernel/k0_fused_moe/benchmarks/mok_synthetic_prefill/run_campaign.sh	2026-08-12 05:33:00.318513132 +0000
+++ /home/subvadla/e36/rc_T.sh	2026-08-12 11:14:58.251290167 +0000
@@ -1,7 +1,7 @@
 #!/usr/bin/env bash
 set -euo pipefail
 
-SCRIPT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
+SCRIPT_DIR="${E36_HARNESS_DIR:-$HOME/amd-master/auto-gpu-kernel/k0_fused_moe/benchmarks/mok_synthetic_prefill}"
 K0_ROOT="$(cd -- "${SCRIPT_DIR}/../.." && pwd)"
 WORKSPACE_ROOT="$(cd -- "${K0_ROOT}/../.." && pwd)"
 HIPKITTENS_ROOT="${HIPKITTENS_ROOT:-${WORKSPACE_ROOT}/HipKittens}"
@@ -16,6 +16,17 @@
 arm_csv="${K0_MOK_ARMS:-production,pf4h}"
 IFS=',' read -r -a base_arms <<< "${arm_csv}"
 
+# ---- exp_36 shape parameterisation (the ONLY behavioural delta vs the stock
+# run_campaign.sh; every default below reproduces the hard-wired value at
+# T=4096, so the T=4096 arm is a true control) --------------------------------
+K0_T="${K0_T:-4096}"
+K0_T_LOC_MAX="${K0_T_LOC_MAX:-$(( 10 * K0_T ))}"
+_pad_safe=$(( 8 * K0_T * 8 + 31 * 32 ))
+K0_PADMAX="${K0_PADMAX:-$(( (_pad_safe + 31) / 32 * 32 ))}"
+K0_MAXTOK="${K0_MAXTOK:-${K0_T}}"
+K0_MAXTOK_PROD="${K0_MAXTOK_PROD:-${K0_T}}"
+echo "[exp_36 shape] T=${K0_T} T_LOC_MAX=${K0_T_LOC_MAX} PADMAX=${K0_PADMAX} MAXTOK=${K0_MAXTOK} MAXTOK_PROD=${K0_MAXTOK_PROD}"
+
 if [[ ! -f "${K0_ROOT}/CLAUDE.md" ]]; then
   echo "k0 root not found at ${K0_ROOT}" >&2
   exit 2
@@ -119,11 +130,11 @@
       -e K0_INPUT_MODE=mok_synthetic \
       -e K0_BENCHMARK_PROTOCOL=mok_eager \
       -e "K0_ARMS=${arms}" \
-      -e K0_T=4096 \
-      -e K0_T_LOC_MAX=40960 \
-      -e K0_PADMAX=263136 \
-      -e K0_MAXTOK=4096 \
-      -e K0_MAXTOK_PROD=4096 \
+      -e "K0_T=${K0_T}" \
+      -e "K0_T_LOC_MAX=${K0_T_LOC_MAX}" \
+      -e "K0_PADMAX=${K0_PADMAX}" \
+      -e "K0_MAXTOK=${K0_MAXTOK}" \
+      -e "K0_MAXTOK_PROD=${K0_MAXTOK_PROD}" \
       -e K0_BLK=128 \
       -e K0_WARP=16 \
       -e "K0_MOK_SEED_BASE=${K0_MOK_SEED_BASE:-1234}" \
